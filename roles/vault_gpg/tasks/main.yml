---
- name: Ensure vault user .gnupg directory exists
  ansible.builtin.file:
    path: "/home/{{ vault_gpg_user }}/.gnupg"
    state: directory
    owner: "{{ vault_gpg_user }}"
    group: "{{ vault_gpg_user }}"
    mode: "0700"
